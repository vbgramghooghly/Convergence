name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/59 * * * *' # Runs every 60 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP request to Streamlit app
        run: |
          curl -I https://hooghlyconv.streamlit.app
